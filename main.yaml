heat_template_version: 2015-04-30

description: Basic single node website

resources:
  my_instance:
    type: OS::Nova::Server
    properties:
      key_name: { get_param: key_name }
      image: { get_param: image_name }
      flavor: { get_param: flavor_name }

  my_network:
    type:
    properties:

  my_router:
    type: 
    properties:

